# 阶段 3 样式 · 步 27

## `style: 圆角、边框、阴影、内存按钮行收尾细节`

- **指令(逐字):** 窗口阴影、边框、内存按钮行小字浅灰(禁用态)、标题栏对齐、整体间距与留白微调。深色主题切换可点击生效。验收:整体与 Win11 标准计算器难辨高下。
- **AI 响应摘要:** `Calculator.vue` 加标题栏(`≡ 标准 ▾` + 主题切换 🌙/☀)+ `theme = ref('light')` + `toggleTheme`(**样式阶段唯一逻辑豁免**,spec §8 认可),根容器 `:class="{ dark: ... }"`;`<style>` 加标题栏规则。**验证**:build 通过;浏览器实测 `8+9=17` 正常、🌙/☀ 切换 `isDark` 生效,浅/深截图留存。
- **改动文件:** `src/components/Calculator.vue`
- **Commit:** `b425f82` `style: 圆角、边框、阴影、内存按钮行收尾细节`
