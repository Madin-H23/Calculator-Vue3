# 阶段 1 布局 · 步 10

## `feat(layout): 组装各组件到主页面并补齐完整 4×6 网格顺序`

- **指令(逐字):** 按网格顺序把所有按钮以正确行列顺序放入 4×6 网格;补齐内存按钮行(MC MR M+ M- MS M▾)作为禁用占位(可用 `disabled` 属性)。验收:页面呈现完整布局骨架(虽无样式),顺序与 Win11 一致。
- **AI 响应摘要:** ButtonPad 拆为**内存行(独立 flex,6 键 disabled)+ 主区 4 列 Grid**,两区分离(spec 决策,避免 6 vs 4 列冲突)。**验证**:浏览器快照确认内存行 6 键 + 主区 24 键,顺序与 Win11 完全一致。
- **改动文件:** `src/components/ButtonPad.vue`
- **Commit:** `d655881` `feat(layout): 组装各组件到主页面并补齐完整 4×6 网格顺序`
